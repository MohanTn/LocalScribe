node_modules/
out/
release/
.whisper-cpp/
vendor/
*.log
# Whisper models are downloaded at runtime into the OS userData dir (see
# src/main/models.ts) — this local model/ folder is a manual/dev-only copy.
model/
